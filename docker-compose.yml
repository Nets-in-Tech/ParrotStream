services:
  postgres:
    image: postgres:16
    container_name: parrotstream-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: parrotstream
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: parrotstream-redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:

